<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".NoteApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Notepad"
        tools:targetApi="31">
        <activity
            android:name=".main.view.show_note.ShowNoteActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main.view.note_list.NoteListActivity"
            android:exported="false" />
        <activity
            android:name=".main.view.edit_note.EditNoteActivity"
            android:exported="false" />
        <activity
            android:name=".main.view.add_note.AddNoteActivity"
            android:exported="false" />
    </application>

</manifest>